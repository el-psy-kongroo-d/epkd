# Optional: Sentry DSN for browser error capture (blank = disabled).
# Vite bakes VITE_* vars in at build time.
# VITE_SENTRY_DSN=

# Optional: GA4 measurement id (blank = analytics disabled; loads only in prod builds).
# VITE_GA_ID=
