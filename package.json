{
  "name": "epkd",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "concurrently -k \"pnpm --filter @epkd/api dev\" \"pnpm --filter @epkd/web dev\""
  },
  "devDependencies": { "concurrently": "^9.1.0", "gray-matter": "^4.0.3" }
}
